# R18 Core 详情动作与响应布局验收

- 状态：开发版验收通过；尚未进入公开安装包。
- 代码版本：Desktop `8889ab62ae8ceae032965ebea1a01cf246bccc15`，PR [#156](https://github.com/PIGU-PPPgu/edupi-desktop/pull/156)。
- Core 版本：公开 `v0.3.18` 资源内的固定 Core `19c0fd5182c6c20d6534973e506d6fa36acc1b06`，组件清单 `sha256:209233c9441de1828b26b6afd4b809ef9e5398404e28ca5daea05d60e52dcd51`。
- 环境：macOS、Next.js development server、Codex in-app browser；教师数据只使用临时副本，包含 50 名学生和 30 份材料索引，验收后已删除。

## 操作与结果

| 操作 | 预期 | 实际 |
| --- | --- | --- |
| 1458×900 打开学生档案 | 修改、AI、删除、关闭为不带可见文字的图标；全局“已删除”不压住动作 | 通过；四个动作有 tooltip 与可访问名称，右上角无重叠 |
| 800×900 刷新同一学生路由 | 主应用占满第一列；对象侧栏不覆盖详情；抽屉不占用底部导航 | 通过；应用宽度 800px，详情覆盖应用内容区，对象侧栏收起 |
| 学生抽屉键盘操作 | 初始焦点进入抽屉；Tab/Shift+Tab 不跳到背后页面；Escape 关闭并清理路由 | 通过；焦点从“关闭学生档案”到“知识图谱”并返回，Escape 删除 `student` 参数 |
| 点击“手动修改”图标后取消 | 打开真实编辑表单，不写入数据 | 通过；班级、学生特征、家校备注和保存动作出现，取消后未提交请求 |
| 800×900 与 1458×900 打开材料详情 | 预览、打开、定位、关联、删除、协作按可用能力显示为图标；长说明不产生横向滚动 | 通过；当前对象显示预览与协作图标，说明自动换行，关闭后返回同一材料分类 |
| 材料列表与“已删除”控件同时出现 | 标题主动作与浮动控件分开 | 通过；标题按控件实际宽度留出右侧空间 |
| 点击数据库下一页图标 | 页码与记录一起更新 | 通过；材料从 1/4 切到 2/4 |
| 浏览器控制台 | 无脚本错误 | 通过；两轮验收 `error` 日志均为空 |

## 命令证据

- `npm test`：1248 tests，1223 passed，25 skipped，0 failed。
- `node_modules/.bin/tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run security:audit`：0 vulnerabilities。

## 未验证条件

- 尚未安装包含 #156 的 Tauri 包，因此原生 28px 拖动标题栏、Windows 和 Linux 窗口仍待安装版验收；窄屏抽屉使用应用内容区内的绝对定位，静态回归锁定其不会覆盖标题栏。
- 普通浏览器没有桌面 API token，`/api/edupi/materials/staging` 在该环境返回 503；本轮已验证目标页面行为与 console error，但不能用此开发环境宣称安装版“无失败请求”。
- 未触发删除、AI 写回或文件打开等会改变状态或依赖原生壳的动作；这些继续沿各自现有验收记录与安装版流程验证。
