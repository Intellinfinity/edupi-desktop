# R18/R21 主动协作与聊天浮层验收

- 状态：开发版真实页面验收通过；尚未进入公开安装包。
- 代码版本：Desktop `4cf2740e6987a9c39eebe6d74c1f827fa7605ff8`，PR [#165](https://github.com/PIGU-PPPgu/edupi-desktop/pull/165)。
- 环境：macOS、Next.js development server、Codex in-app browser、已安装 `v0.3.19` bundled Core；教师数据临时副本，验收后删除。

## 操作与结果

| 操作 | 预期 | 实际 |
| --- | --- | --- |
| 在 AI 协作打开有产物的历史会话 | 右上只保留主动协作与产物图标，不显示回收站 | 通过；产物徽标为 2，垃圾桶消失 |
| 打开“本次产物” | 浮层覆盖消息，不参与聊天文档流 | 通过；显示两份真实 HTML；打开前后消息滚动区高度均为 720px，`scrollTop=5597.5` 不变 |
| 按 Escape | 关闭浮层并把焦点还给入口 | 通过 |
| 打开主动协作 | 显示真实 Core 主动状态和对象动作 | 通过；显示 3 条待处理提醒、早安简报和课前检查运行；失败项进入“补充材料” |
| 点击一个“补充材料”动作 | 进入对应恢复入口 | 通过；URL 和页面切到材料模块 |
| 点击左侧主动协作状态 | 同样打开聊天内主动浮层 | 通过 |
| 管理中心 → 回收站 | 删除记录不占工作台，技术信息默认隐藏 | 通过；读取 5 个可恢复 Core tombstone，ID 位于折叠的“技术详情” |
| 1458×900、800×900 视口 | 图标、浮层、聊天输入和导航无重叠 | 通过 |
| 控制台与优化后请求 | 无脚本错误或 5xx | 通过；console error 0，最终请求批次均为 2xx |

## 命令证据

- `npm test`：1255 tests，1230 passed，25 skipped，0 failed。
- `node_modules/.bin/tsc --noEmit`：通过。
- `npm run lint`：通过。
- `npm run security:audit`：0 vulnerabilities。

## 未验证条件

- 本轮没有恢复真实对象；恢复能力沿用现有 Core 回执接口，页面只在临时副本中读取记录。
- 当前公开 `v0.3.19` 不包含 #165；原生 Tauri 窗口、Windows 与 Linux 留待下一发布批次。
- 主动协作展示现有 Core 运行和提醒，不新增 Core 触发器；真实睡眠唤醒、系统通知点击及自动补跑仍按 R21 验收。
