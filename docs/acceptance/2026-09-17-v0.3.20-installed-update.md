# R18/R21/R22 v0.3.20 安装更新验收

- 状态：macOS 安装版原位升级与本次界面改动验收通过；Windows/Linux 升级、系统通知点击和睡眠唤醒仍未验收。
- 代码版本：功能 PR [#165](https://github.com/PIGU-PPPgu/edupi-desktop/pull/165)，merge commit `18cd6be232737a6c0972083065f08945b371b07d`；发布 PR [#166](https://github.com/PIGU-PPPgu/edupi-desktop/pull/166)，Release commit `3b173f6a95cb566bc69188542a5268d44c11c199`。
- 环境：macOS Apple Silicon、原位更新的 `/Applications/EduPi.app`、公开 Latest `v0.3.20`、真实教师数据；未向真实学生写入测试数据。

## 操作与结果

| 操作 | 预期 | 实际 |
| --- | --- | --- |
| 从运行中的 0.3.19 执行应用内更新 | 下载验签、原位替换并重启，不留下第二个应用副本 | 通过；重启后 Info.plist 和组件清单均为 0.3.20，`/Applications` 只发现一个 `EduPi.app` |
| 核对更新接口 | 当前版与公开 Latest 一致，且不再提示更新 | 通过；当前 0.3.20、最新 0.3.20、`updateAvailable=false`、状态 available |
| 核对发布产物 | 三平台构建、签名和 manifest 完整后公开 | 通过；workflow 35210481151 的 release、Windows/Linux/macOS build 和 manifest 均 success，11 项资产；`latest.json` 版本 0.3.20 且 7 个平台键均有签名 |
| 核对组件身份 | Desktop/Pi/pi-web 版本随包更新 | 通过；Desktop 0.3.20、Pi 0.84.1、pi-web 0.8.7；Core commit `19c0fd5182c6c20d6534973e506d6fa36acc1b06` |
| 核对 Core 与真实数据 | 升级不丢教师、对象和模型配置 | 通过；Core/projection/kernel 均 ready；吴老师/数学/七年级，50 名学生、240 个任务、43 个校历节点、9 个课表；默认模型 `zai-coding-cn/glm-5.3-flash`、13 个可见模型；45 条记忆与 20 份产物在升级后回读，回收站 5 个可恢复对象和 5 条历史 |
| 检查聊天右上角与回收站 | 聊天不再显示回收站，删除记录进入管理中心 | 通过；聊天右上只保留主动协作和本次产物，管理中心回收站显示真实 tombstone，技术详情默认收起 |
| 检查本次产物浮层 | 产物覆盖显示，不挤压聊天内容 | 通过；浮层显示 2 份真实 HTML，支持打开所在目录，Escape 可关闭且聊天滚动位置不变 |
| 检查主动协作 | Core 的提醒、运行、失败和完成状态可见并可行动 | 通过；左侧主动状态可打开同一浮层，显示 3 条待处理提醒和 Core run；“补充材料”进入材料模块 |
| 检查服务日志 | 升级后无新的服务端错误 | 通过；最近 server.log 尾部无 error、exception、500 或 503 匹配 |

## 命令证据

- `gh api repos/PIGU-PPPgu/edupi-desktop/actions/runs/35210481151/jobs`：release、三个平台 build、manifest 均 success；notify 为预期 skipped。
- `gh api repos/PIGU-PPPgu/edupi-desktop/releases/tags/v0.3.20`：11 项公开资产，目标提交 `3b173f6a95cb566bc69188542a5268d44c11c199`。
- 下载 Release `latest.json` 并解析：7 个平台键均含签名。
- `/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' /Applications/EduPi.app/Contents/Info.plist`：0.3.20。
- `curl http://127.0.0.1:38472/api/updates`：0.3.20 / 0.3.20 / false / available。
- `curl http://127.0.0.1:38472/api/edupi/status`：Core、projection、kernel 均 ready，数据计数 50/9/43/240。
- `curl 'http://127.0.0.1:38472/api/models?cwd=<本机 EduPi 数据根>'`：默认模型 `zai-coding-cn/glm-5.3-flash`，13 个可见模型。
- `codesign -dv /Applications/EduPi.app`：仍为 ad-hoc 签名，TeamIdentifier 未设置。
- 发布前全量：`npm test` 1255 tests、1230 passed、25 skipped、0 failed；`node_modules/.bin/tsc --noEmit`、`npm run lint`、`npm run security:audit`、`cargo metadata --locked --no-deps`、`npm run release:verify` 通过。

## 未验证条件

- Windows 和 Linux 旧版没有执行应用内升级，不能用 macOS 结果替代。
- 未触发并点击真实系统通知；睡眠唤醒后的幂等补跑仍归 R21。
- macOS 二进制仍为 ad-hoc 签名，没有 Developer ID 和公证；辅助功能、屏幕录制等系统授权的跨版本有效性不能因此判定。
- 本轮检查的是 1458px 与 800px 开发视口及 macOS 安装版目标交互；安装版完整 Tab 遍历矩阵未全部复跑。
