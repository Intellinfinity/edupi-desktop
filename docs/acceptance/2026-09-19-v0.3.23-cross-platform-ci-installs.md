# v0.3.23 跨平台 CI 安装验收

## Linux

- 运行：[35406232707](https://github.com/PIGU-PPPgu/edupi-desktop/actions/runs/35406232707)，workflow `Verify the published Linux desktop build`。
- 环境：Ubuntu 24.04 runner、干净临时 HOME/数据根、Xvfb 1280×900。
- 结果：从公开 Latest 下载 `EduPi_0.3.23_amd64.deb`，dpkg 版本为 0.3.23；安装后启动 packaged app，轮询真实 loopback server 直到 Core 与 education projection 均 ready，结论 success。

## Windows

- 运行：[35406231497](https://github.com/PIGU-PPPgu/edupi-desktop/actions/runs/35406231497)，workflow `Debug the Windows desktop build`，输入 tag `v0.3.23`、native_only true。
- 环境：windows-latest runner。
- 结果：published-install 下载并安装公开 NSIS 包、启动应用；使用包内 public resource fixture 覆盖源码资源后执行 `cargo check --lib --locked`。job 结论 success，诊断 job 按输入跳过。

## 边界

- 两项都是 GitHub runner 的真实安装启动证据，不等同于教师学校里的 Windows/Linux 人工实机升级。
- R22 仍保留目标学校设备、真实旧版应用内升级、外设/杀毒软件/账号环境与用户验收。

