# v0.3.29 更新、Safe Mode 与手机桥接验收

## 2026-09-22 Developer ID 签名与公证链路补验

- Apple Developer 已签发 `Developer ID Application` 证书，证书主体为 `Developer ID Application: LinZhang Wu (BY3XZG2839)`，有效期至 2031-09-17；本机钥匙串 `security find-identity -v -p codesigning` 回读 1 个有效身份。受限本机 signing 目录中的 `developer-id-application.p12` 权限为 `600`，p12 内含 leaf 与 `Developer ID G2` 中间证书，密码只存 macOS 钥匙串与 GitHub Secret。
- GitHub Actions 已配置 `APPLE_CERTIFICATE`、`APPLE_CERTIFICATE_PASSWORD`、`APPLE_SIGNING_IDENTITY`、`APPLE_ID`、`APPLE_PASSWORD`、`APPLE_TEAM_ID` 六个 Secret；`xcrun notarytool history` 使用同一 Apple ID、Team ID 和应用专用密码完成过认证查询，未回显凭据。
- 远端功能分支已通过 Git Data API 推送资源签名修复提交 `783faf6`。新增 macOS 资源签名步骤会在 Tauri 外层签名前，对 packaged server/Core 中的 Mach-O、`.dylib`、`.node` 与 helper app 使用同一 Developer ID、hardened runtime 和 secure timestamp 签名；针对性测试通过。
- 第一次 `v0.3.30` run `35622049814` 因 Secret 写入时把 `-` 当作字面值而在 base64 解码阶段失败；第二次 `35625498392` 已通过证书导入但 Apple 拒绝未带有效链/时间戳的嵌套 native binaries；对应 notarization IDs `4aeaa219-32cf-4e2d-9510-13da07f55a8b`、`fc5a44ef-3d6d-4b3f-8bc7-77468c17aba0` 均为 `Invalid`。
- 第三次 run `35629102171` 使用带 G2 中间证书的 p12，Linux/Windows 成功，Apple submission `5c9fa88f-91b1-4061-aca3-04efca7eec03` 长时间 `In Progress` 后从 Apple 查询中消失；该 run 已取消。第四次 run `35632713805` 验证资源签名步骤后因同一外部队列阻塞取消。
- 最终重跑 `35676436480` 全绿：macOS 资源签名报告 `Signed 3 native resource(s) and 1 helper app bundle(s)`；Apple submission `3e2d7f01-39bd-430f-8260-454d0df6bc28` 返回 `Accepted`，Tauri 日志记录 `Stapling app...`。Release 已发布为 Latest `v0.3.30`，11 个资产齐全，feed 有 7 个平台键。
- 公开 DMG 下载后本机 `codesign --verify --deep --strict` 通过，证书链为 Developer ID Application → Developer ID Certification Authority → Apple Root CA，带 secure timestamp；`spctl --assess --type execute` 返回 `accepted / source=Notarized Developer ID`。本机 `stapler validate` 受当前 Apple CloudKit TLS `-1200` 阻断，但 CI 已完成 Tauri stapling。

## 2026-09-21 对话输入与 Core Runtime 修复补验

- Desktop 修复提交 `2e36729`：流式期间的“引导 / 后续消息”收进可访问的三点菜单，停止动作改为无文字方形图标；输入区加号提供图片和文件附件，文件沿现有项目导入链路并插入 `@` 路径；桌面主导航左下增加带 tooltip/ARIA 名称的手机图标，桌面端定位到手机桥接设置，网页端打开 `/mobile`。
- Core Runtime 恢复增加桌面 token 保护、双 SQLite `BEGIN IMMEDIATE`、WAL 在线备份、精确 schema/metadata/authority 校验、跨提交 repair intent；缺失状态库 fail-closed，死 owner 的 admission lease 可清理，活跃 owner/lease 仍拒绝修复。回归测试覆盖 15 项 runtime recovery 场景，route token 边界测试通过。
- 新包由 pinned Core `d05cf89df067a78602883c6bd95a37f8b0c122f7` 构建；生产 server 枚举到 `/api/edupi/runtime/repair`，安装版 `127.0.0.1:38471` 返回 HTTP 200，Core/projection/kernel 回读 `ready/ready/ready`，Core 计数为 51/9/43/240；隔离 `memory-update` E2E 返回 `status=passed`，未触碰教师源文件；未授权 repair 请求返回 403。
- 安装前旧包备份位于 `/private/tmp/edupi-before-local-fix-20260921/EduPi.app`。新包已替换 `/Applications/EduPi.app` 并启动；当前 macOS 锁屏，无法完成新包窗口的鼠标/AX 视觉点击复核，不能将三点菜单、加号菜单和左下手机图标记为安装版视觉验收通过。
- 本次本地 Tauri bundle 生成 `.app`、DMG 和 updater tar.gz；因环境没有 `TAURI_SIGNING_PRIVATE_KEY`，命令最后在 updater 签名步骤退出，未宣称为可发布签名包。
- 公开 Latest `v0.3.29` 仍对应此前发布提交；本批 `2e36729` 尚未进入公开 Release，因此现有已安装/公开客户端不会通过同版本 updater 自动获得这些修复。需后续 bump 到新版本并完成签名发布后，才能做公开升级验收。

### 解锁后的安装版补验与 0.3.30 候选

- macOS 解锁后，安装版真实窗口确认左下主导航和输入区均有“手机控制”；两处点击都打开设置并定位到“手机继续对话”。加号菜单真实展开“附加图片 / 添加文件”，Escape 关闭。流式运行时真实显示三点按钮与无文字方形停止键，三点菜单展开为“引导 / 后续消息”。专用验收会话随后经精确 session id 删除，API 回读 404，应用重启后不再出现。
- 手机入口启用后监听从 `127.0.0.1:38471` 变为 `*:38471`，iPhone Simulator 打开 LAN `/mobile` 页面；配对请求、桌面批准和 HttpOnly cookie 完成，受权客户端回读 100 个会话与 20 条提醒。验收后撤销 pairing 并关闭入口，监听恢复 `127.0.0.1:38471`，LAN `/mobile` 不可达，旧 cookie 返回 401。
- 版本元数据已从 `0.3.29` 推进为 `0.3.30`，Cargo、组件清单和 release notes 一致；`release:verify`、36 个 release/updater 定向测试、1353 个全量测试、TypeScript 与 lint 通过。候选提交为 `d62f3cf`。
- 上述“凭据尚未配置”是本节早期状态，已由最终 `v0.3.30` 发布证据取代。

## 版本与发布

- Desktop PR [#200](https://github.com/PIGU-PPPgu/edupi-desktop/pull/200) 通过 `audit` 与 `rust-audit` 后合并，merge commit 为 `a7b83a90735456f12ae306c8888b4e04351a229b`。
- Release workflow [35548760372](https://github.com/PIGU-PPPgu/edupi-desktop/actions/runs/35548760372) 的 macOS、Linux、Windows 构建、固定 Core 配对、桌面壳测试和签名资产上传均成功。首次 manifest 因 `updater-feed` 尚不存在而保持 draft；创建专用分支后只重跑失败 job，attempt 2 成功，未重建或替换三平台资产。重复触发的全量 run `35553874368` 已取消。
- 正式 Latest [v0.3.29](https://github.com/PIGU-PPPgu/edupi-desktop/releases/tag/v0.3.29) 非草稿、非预发布，共 11 个资产。Raw feed 为 `https://raw.githubusercontent.com/PIGU-PPPgu/edupi-desktop/updater-feed/latest.json`，版本 `0.3.29`，有 7 个平台键和 4 个唯一 GitHub API asset URL。
- 首次 feed 分支从 main 建立，当前树错误包含源码；随后以父 commit `9b3324f…` 创建快进 commit `7ae9dd2…`，新树 `5dc7e72…` 恰好只有 `latest.json`，Raw 内容与已发布 manifest 相同。后续 workflow 改为 Git Data API 直接创建一文件树，分支不存在时创建 root commit，存在时只做 `force=false` 快进。
- macOS DMG asset `578053144` 为 76,155,421 字节，GitHub digest 和本地 SHA-256 均为 `40764478e340df9445df1c097c6b9a5ce948496b29837fbc58c7666697010e25`。挂载校验通过，应用为 arm64、bundle id `com.abcwyc.pi-agent`、版本 `0.3.29`；签名仍为 ad-hoc。API asset endpoint 带 `Accept: application/octet-stream` 返回有效 1024 字节 ELF 分片。

## macOS bootstrap 与数据保持

- 升级前唯一 `/Applications/EduPi.app` 为 `0.3.28`，真实更新接口返回 current `0.3.28`、latest `0.3.29`、`updateAvailable=true`。旧应用先退出并备份到 `/tmp/edupi-v0329-bootstrap.msVYdn/replaced-EduPi-0.3.28.app`，再用已校验 DMG 原位替换；没有创建第二个 Applications 安装副本。
- 升级后 Info.plist 和界面管理中心均为 `v0.3.29`；更新接口返回 current/latest `0.3.29`、`updateAvailable=false`。只有一个主进程和一个 packaged Next server，`/Applications` 与用户 Applications 中只发现一个 `EduPi.app`。
- 升级前后稳定计数一致：51 名学生、238 个 rhythm 任务、43 个校历事件、9 个课表时段、26 条记忆、146 个数据文件、3 个模型 Provider、9 个配置模型。Core 教育投影回读为 51/240/43/9；240 包含 rhythm 任务之外的手工任务，因此不与 238 混用。
- 学生、校历、课表、记忆、`models.json`、`settings.json` 和 `auth.json` 的逐文件 SHA-256 升级前后完全一致；默认模型保持 `zai-coding-cn / glm-5.3-flash`。
- Core 身份回读仍为固定 commit `d05cf89…`、合同 `1.1`、11/11 命令和 `education_workspace`。Core Runtime 的 `runtime_root_invalid` 在 0.3.28 升级前已存在，0.3.29 后仍存在；教育投影与主动运行内核为 ready。该既有数据根问题没有因本次升级扩大，也不能记录为 Core Runtime ready。

## Safe Mode

- 以 `/Applications/EduPi.app/Contents/MacOS/pi-agent-desktop --safe-mode` 冷启动，`/api/desktop/runtime` 回读 `safeMode=true`；原生窗口显示“安全模式 / 第三方扩展已暂停 / 恢复正常启动 / 查看诊断”。教师上下文、Today 投影和基础协作界面仍可读取。
- 无参数重新启动后 `/api/desktop/runtime` 回读 `safeMode=false`，版本仍为 0.3.29。Orca 能读取恢复按钮，但一次点击因 macOS AX 临时拒绝未确认；本轮用等价的退出后无参数启动完成恢复，不把该次点击写成通过。
- 启动诊断为空，没有保存 API Key、Bearer token 或完整异常堆栈。

## 手机继续对话

- 在安装版设置中点击“启用手机入口”，应用实际重启；偏好为 `mobileBridgeEnabled=true`，server 从 loopback 改为 `*:38471`。LAN 地址 `192.168.31.99` 上 `/mobile` 返回 200，根页面和 `/api/sessions` 均返回 403。
- 桌面设置生成 8 位配对码；LAN 客户端以“安装验收手机”请求连接，桌面出现“批准”。批准后客户端收到 `active` 和 `mobile:read/mobile:chat`，token 只进入 HttpOnly、SameSite cookie，不出现在 JSON 或 localStorage。
- 已配对客户端经 LAN 回读 100 段会话和 20 条提醒；桌面设置显示“安装验收手机 · 已连接 / 撤销”。撤销后同一 cookie 请求立即返回 401。
- 验收结束将手机入口恢复为关闭并正常重启；server 重新只监听 `127.0.0.1:38471`，LAN `/mobile` 不可达。教师数据与模型哈希保持不变。
- 390×844 手机连接页和原生 800×900 设置页完成像素检查：输入、按钮、配对状态和 JEV 设置没有横向溢出或互相遮挡；1440×900 原生工作台显示 v0.3.29。

## 未验证边界

- 本轮 LAN 配对使用同一 Mac 的 LAN 地址模拟手机客户端；真实第二台 iOS/Android 设备、Wi-Fi 切换、锁屏恢复和不同路由器尚未验收。
- v0.3.29 已内置新 Raw/API updater 链路，但要到存在 v0.3.30 时才能完成“新客户端检查 → 下载 → Tauri 验签 → 原位安装”的下一跳实测。本次完成的是旧 v0.3.28 的一次手动 bootstrap。
- Windows/Linux 有本次 workflow 的干净构建和 Rust 测试证据，没有真实设备上的应用内升级证据。Apple Developer ID、公证和 TCC 跨版本保持仍未具备。
