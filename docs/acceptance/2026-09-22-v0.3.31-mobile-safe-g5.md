# v0.3.31 手机隔离与 G5 配对验收

## 范围与环境

- Desktop `0.3.31` 源码、macOS arm64 隔离 standalone 构建；Core detached `7cbb280eaf33bdb259e719a0149b7f2ca343d356`，数据只在 `/tmp` 临时根。
- G5 仍为 Bridge v1.1、12 个已支持命令、`education_workspace`；不启用家校外发或把合成反馈计入真实效果。

## 已验证

| 操作 | 预期与实际结果 |
| --- | --- |
| 定向网关测试与真实双接口探针 | Next loopback `/api/home` 为 200；具体 LAN IP 的 `/mobile` 为 200；伪造 `Host: 127.0.0.1` 访问 `/api/sessions` 和 `/api/mobile/pairing` 均为 403；网关拒绝编码绕过、跨站请求及越权方法。 |
| 隔离手机配对 | 实际生成配对码、在 LAN 接口请求、桌面令牌批准、手机 Cookie 查询到标记测试会话：配对、会话读取均 200；loopback 页面批准后无刷新立即列出会话并读到两条消息。未写真实教师数据。 |
| `node --test lib/safe-mode.test.mjs` | Core manifest 内 Skill 可用，教师目录中自定义 Skill 即使误传给 SDK 仍不可见；正常模式路径未变。 |
| `EDUPI_CORE_ROOT=<G5> npm run test:edupi-c1-e2`、`test:edupi-c2-e2`、`test:edupi-c3-e2`、`test:edupi-follow-up-e2` | Core 身份、schema、回执、幂等和重启读回通过；`external_send=false`。 |
| `EDUPI_CORE_ROOT=<G5> npm run test:edupi-calendar-dedupe-e2` | 两次顺序不同的相同导入保持 2 个稳定对象，未定日期保持待核。 |
| `EDUPI_CORE_ROOT=<G5> npm test` | 1370 通过、9 跳过、0 失败；无 Core 的 Release 前置环境为 1354 通过、25 跳过、0 失败。跳过项不计入验收。TypeScript、ESLint、npm audit 均通过。 |
| `EDUPI_CORE_ROOT=<G5> npm run desktop:prepare` 于独立 Desktop worktree | 生成 Next/Node/Core 的 G5 生产资源，Core 2159 文件；没有覆盖主工作区正在运行的 `.next`。 |
| `EDUPI_STAGED_RESOURCES=<隔离资源> npm run test:staged-desktop-runtime` | 冷启动 Core/投影 ready，G5 身份和资源 hash 匹配，主动运行默认关闭，`externalSend=false`，网关文件存在。 |
| `EDUPI_STAGED_RESOURCES=<隔离资源> npm run test:staged-feedback-runtime` | 未授权 GET/POST 均 403；授权写入、精确重放和读回通过，合成记录不进入真实价值指标。 |
| `EDUPI_STAGED_RESOURCES=<隔离资源> EDUPI_CORE_ROOT=<G5> node --test scripts/runtime-model-host-files.test.mjs` | 完整 Pi SDK 依赖闭包中的模型 host 独立执行 localhost 模型一次，产物回读、凭据不进入结果；嵌套 `retry@0.12` 与顶层 `retry@0.13` 保留各自解析路径。 |

## 状态边界

- 上述是源码和隔离生产资源验收，不是公开 0.3.31 安装或自动更新成功。当前公开 0.3.30 的手机入口仍存在 Host 伪造风险，修复发布并升级前应关闭该入口。
- 原生设置页批准/撤销返回 403/500 的视觉错误态、Safe Mode 故障插件实启、第二台手机与网络切换、系统通知点击、真实睡眠唤醒、Windows/Linux 旧版升级均未运行。
- Apple 应用已在 v0.3.30 通过 Developer ID 公证；新 workflow 的 fail-closed 和 staged 门禁须在 0.3.31 Actions 实跑，三平台资产、feed、签名与旧客户端升级分别核对。
- 本机安装升级前为 v0.3.29、Core `d05cf89`，Core/投影 ready；工作区回读 51 名学生、240 项任务、43 条校历、9 项课表。通用 `/api/models` 因 cwd allow-list 返回 403，尚不能作为模型配置基线。该安装版更新接口能检测到公开 v0.3.30；待 v0.3.31 发布后重新检查、验签安装和重启读回。
- 仓库迁移后，旧 `PIGU-PPPgu` raw feed 与新 `Intellinfinity` raw feed 均返回 200；Release 写入与 Core checkout 已切换 canonical 路径，客户端已编译的旧 raw endpoint 保留兼容。
