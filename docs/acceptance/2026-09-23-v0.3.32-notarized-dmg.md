# v0.3.32 公证 DMG 与 canonical feed 验收

## 发布身份

- Desktop PR [#211](https://github.com/Intellinfinity/edupi-desktop/pull/211) 从精确 head `fc5633788e239249bc35f3c8e673d5e3a9355fc4` 合并为 `9f33463fc5298ea54a361e809419f8d315c19b22`；正式版本为 `0.3.32`，Core 仍固定已验收的 G6 `acce81e3b59ae93a998e7c1e9d1f008059ec3e85`。
- 首轮全平台 run `35771039553` 的 Linux、Windows 成功，macOS 在构建前的 G6 临时数据根 writer-admission 检查返回 `writer_admission_invalid_root`，manifest 正确跳过，Release 保持 draft。相同 Core/测试在 v0.3.31 macOS runner 成功，本机连续五次成功，本轮 Linux 同样成功；因此只重试同一提交的 macOS，不修改合同或跳过门禁。
- macOS 单平台 run `35772209888` 通过 G6 配对、staged server/Core/反馈/模型 host、签名、公证、DMG 事务和 manifest。最终 Release ID `394034871`，tag `v0.3.32`，target `9f33463fc5298ea54a361e809419f8d315c19b22`，非草稿、非预发布，共 11 项资产，没有遗留 backup 资产。

## DMG 公证与资产事务

- DMG 的 Apple submission `500735b4-4a03-4b0b-8376-c6cf3270c48c` 返回 `Accepted`。runner 对同一 DMG 的 `stapler staple`、`stapler validate` 与 Gatekeeper 均通过，随后按 Release ID `394034871` 替换资产，并记录 SHA-256 `3ee2667bccd72b9c182baab4136b65e54a54aa5c31a4365ba6e4d279f2e21884`。
- 事务在变更前核对 release ID、唯一 tag、draft 和 target SHA；旧资产先改名保留，新资产经 `uploads.github.com/.../releases/394034871/assets` 上传并重新下载核对 size/SHA-256，成功后才删除备份。41 项定向测试覆盖上传失败、哈希不符、中断恢复、DELETE 响应丢失和 cleanup 不确定性；独立复审未发现 P1/P2。
- 从公开 Release 重新下载后，DMG、updater tar、签名、`latest.json`、组件清单的本地 SHA-256 分别与 GitHub digest 一致：`3ee2667b…`、`20a443ff…`、`8122e762…`、`8f9926f8…`、`149c8223…`。DMG 内应用和 updater tar 内应用均为 arm64 `0.3.32`，深度代码签名通过，Gatekeeper 返回 `accepted / Notarized Developer ID`。
- 本机 `xcrun stapler validate` 仍在 Apple CloudKit 请求处被代理 TLS `-1200` 阻断；本机没有把这次命令记为通过。runner 对替换前同一摘要 DMG 的两次 `stapler validate` 均成功，公开下载后的 Gatekeeper 也识别为 `Notarized Developer ID`。

## Feed 与组件

- canonical Raw feed 为 `0.3.32`，包含 `darwin-aarch64`、macOS app、Linux AppImage/deb、Windows NSIS 共 7 个键，全部有签名；4 个唯一 updater URL 均为 `api.github.com/repos/Intellinfinity/edupi-desktop/releases/assets/{id}`。
- `component-versions.json` 为 Desktop `0.3.32`、Pi `0.84.1`、pi-web `0.8.7`；客户端首选 `Intellinfinity` Raw feed，旧 PIGU Raw 与 Release URL 继续作迁移 fallback。

## 安装前基线与边界

- 本机唯一安装副本仍为 `/Applications/EduPi.app` `0.3.29`；代理环境启动的同一路径进程监听 `127.0.0.1:38471`。真实更新接口已返回 current `0.3.29`、latest `0.3.32`、`updateAvailable=true`。
- 升级前 Core/投影/Kernel 均 ready，Core `d05cf89df067a78602883c6bd95a37f8b0c122f7`、Bridge `1.1`、11 个旧命令、`externalSend=false`；工作区为 51 名学生、240 项任务、43 条校历、9 项课表。`auth.json`、`models.json`、`settings.json` 摘要分别为 `093af081…`、`37e90c3a…`、`abe47833…`，手机桥接为关闭。
- Mac 当前锁屏，Orca 与原生 UI 通道均不能读取 EduPi 窗口；因此尚未点击应用内更新，未核对重启后版本、Core G6、数据/模型摘要和唯一副本。不能以公开 DMG 或 API 检测代替这一步。
- 通知点击、真实睡眠唤醒、Safe Mode 故障插件、真实第二台手机、Windows/Linux 旧版应用内升级、JEV 实服/受管浏览器/Core Receipt 与 OpenConnector 安装版 sidecar 仍未验收。
