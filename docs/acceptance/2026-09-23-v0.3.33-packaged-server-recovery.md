# v0.3.33 packaged server 恢复验收

当前状态（2026-09-23）：本记录只保留当时的构建与 Linux/Windows 安装证据。随后对公开 macOS updater tar 内的签名 Node helper 执行最小 V8 命令，以 SIGTRAP 退出；v0.3.33 已改为 draft。缺失的 JIT entitlement 已在 [v0.3.35 恢复验收](2026-09-23-v0.3.35-signed-macos-recovery.md)中修复并完成安装。

## 修复与回滚

- v0.3.31、v0.3.32 的 installed Linux/Windows 日志均证明 packaged server 缺少 `next`；两版已退回 draft，v0.3.30 曾恢复为 Latest，feed 以非强制快进 commit `446a73a` 回滚。
- Desktop PR [#213](https://github.com/Intellinfinity/edupi-desktop/pull/213) 修复普通 standalone `node_modules` 被排除的问题，保留 worktree symlink/NFT 展开，最终产物递归拒绝 symlink，必需文件必须为 staged 根内普通文件。PR [#214](https://github.com/Intellinfinity/edupi-desktop/pull/214) 将模型 host 门禁改为真实 Node 解析：Pi SDK 保持 `retry@0.12`，根依赖保持 `retry@0.13`，均不得逃出包目录。
- Release 三个平台在上传前都把 server/Core 复制到仓库外临时根并启动 bundled Node；Windows 验证 Next identity，Linux/macOS 还必须回读 Core/投影 ready。macOS 的反馈、occurrence 前置版本、模型 host 和公证门禁继续保留。

## 正式发布

- 正式 run `35789747783` 固定 Desktop merge `384e952a54aa606dadad232626539999ccb54f04`；macOS、Linux、Windows、manifest 全部成功。Release ID `394151776`，v0.3.33 非草稿、非预发布，11 项资产、7 个签名 updater 键完整；v0.3.31/v0.3.32 继续为 draft。
- DMG submission `2a5b8e12-9340-4ca2-b6f9-7dd78f137862` 为 Accepted；runner 的 staple/validate、Gatekeeper 和替换后复核全部通过。事务按 Release ID `394151776` 重传 DMG，SHA-256 `49d18c959e63e1d71046c6a8aa7cabcc7929838f92c8ffd9c855e38c915930d6`。
- 公开 DMG、updater tar、签名、`latest.json`、组件清单的独立下载摘要分别为 `49d18c95…`、`1c59d2e0…`、`99b922ec…`、`0ab84468…`、`b3c813d1…`，与 GitHub digest 一致。DMG 和包内应用 codesign 通过，Gatekeeper 均为 `Notarized Developer ID`；本机 `stapler validate` 仍受 Apple CloudKit TLS `-1200` 阻断，runner 同摘要验证作为 ticket 证据。
- canonical Raw、旧 Raw 与旧 GitHub Latest endpoint 均返回同一份 v0.3.33 manifest，SHA-256 `0ab84468…`，7 个平台键且无空签名。

## 启动与安装

- 公开 updater tar 内 `next/package.json` 和 Next start-server 存在，server 树无 symlink。直接从仓库外解包目录用系统 Node 启动同一 `desktop-server.cjs`，identity HTTP 204；进一步回读 Core/投影/Kernel 均 ready，Core `acce81e3b59ae93a998e7c1e9d1f008059ec3e85`、12 个命令、`externalSend=false`。签名 helper Node 直接读取 `/tmp` 隔离根在本机 TCC 边界内无日志超时，该条未记为通过。
- Linux public install run `35792864973` 在 Ubuntu 24.04 安装 deb 并于 Xvfb 启动，Core/投影 ready。Windows run `35792875124` 安装器退出 0，应用保持运行、工作区 HTTP 200，第二次启动复用唯一进程；diagnose job按 `native_only=true` 跳过。
- 本机唯一安装副本仍为 v0.3.29，更新接口已检测 v0.3.33；升级前为 51 名学生、240 项任务、43 条校历、9 项课表，模型/认证/设置摘要已记录，手机桥接关闭。Mac 仍锁屏，尚未执行原生点击、Tauri 验签安装、重启与数据保持复核。

## 后续版本边界

- v0.3.33 发布后 Desktop PR #212 才合并到 main `1473bc5`，将 Core pin 更新到 `860594a05c5d32617fffdbdf03d56e6ade6dc211` 并加入 occurrence v1.2/显式冲突审核。因此 v0.3.33 只包含已验收的 G6 包；main 的新合同必须作为 v0.3.34 重新构建，不能把 merge 等同于已发布。
- 通知点击、真实睡眠唤醒、macOS 应用内升级、真实第二台手机、Windows/Linux 旧版应用内升级、JEV 实服/受管浏览器/Core Receipt 与 OpenConnector 安装版 sidecar 仍未验收。
