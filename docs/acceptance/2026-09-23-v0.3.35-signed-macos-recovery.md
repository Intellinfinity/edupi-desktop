# v0.3.35 macOS 签名启动恢复与安装验收

后续 v0.3.35 → v0.3.36 已从安装版完成真实应用内下载、验签、替换与重启，见 [v0.3.36 updater 验收](2026-09-23-v0.3.36-in-app-updater.md)。以下保留 v0.3.35 初次手动 bootstrap 的历史证据。

## 修复边界

- v0.3.29 的应用内更新返回 `plugin updater not found`；v0.3.30、v0.3.33、v0.3.34 的签名 Node helper 缺 `com.apple.security.cs.allow-jit`，实际启动在 V8 初始化处 SIGTRAP。已撤回的版本、Release ID、feed 回滚和本机 v0.3.29 数据恢复见 [v0.3.34 安装回滚](2026-09-23-v0.3.34-core-occurrence-release.md)。
- Desktop [#218](https://github.com/Intellinfinity/edupi-desktop/pull/218) 合并为 `e88f084dbeaf532df9c93d986801c92253772182`：只给 `Pi Agent Server.app` 加 `allow-jit`，资源签名后运行 V8 命令，Tauri 最终 `.app` 上传后、公证前再运行签名 Node 和隔离 Core。源代码与发布记录均保留此前 Core `860594a05c5d32617fffdbdf03d56e6ade6dc211`、桥接 v1.1、可选 occurrence v1.2。

## 发布与公开资产

- 正式 run [35815288358](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35815288358) 的 macOS、Linux、Windows、manifest 均成功。macOS 的 `Sign bundled macOS native resources` 和 `Verify signed macOS packaged runtime` 均通过；后者从最终 `.app` 回读 Core/投影 ready、Core `860594a…`、occurrence 1.2、`externalSend=false`。
- Release [v0.3.35](https://github.com/Intellinfinity/edupi-desktop/releases/tag/v0.3.35) ID `394282553` 非草稿、非预发布、绑定 `e88f084…`，有 11 项资产。Apple DMG submission `f9b1d9ff-93cc-4374-a36f-77061c6e7653` 为 Accepted；runner 对最终 DMG 完成 staple、validate、Gatekeeper 和按 Release ID 替换。最终 DMG SHA-256 `b766f9be955911ed9b469b40791ad18d1a7b42fa59fffb544fc75281076bcf03`。
- 公开独立下载的 DMG、updater tar、签名、`latest.json`、组件清单 SHA-256 与 Release digest 逐一一致，依次为 `b766f9be…`、`61eb9561…`、`29f93cb4…`、`d306677b…`、`966020ec…`。canonical Raw、旧 Raw、旧 GitHub Latest 三个 endpoint 返回同一份 v0.3.35 manifest，7 个平台键、签名全非空；feed commit `710e4e3336bf68cb4144ea46c220e79e441975e8`。
- 从公开 DMG 只读挂载的 `.app` 版本为 0.3.35，深度代码签名和应用/DMG Gatekeeper 均为 `accepted / Notarized Developer ID`。签名 helper 的 entitlement 回读只有 `allow-jit=true`，实际 `node -e` 成功；从该 DMG 的资源运行 `npm run test:staged-desktop-runtime`，Core/投影 ready、occurrence 1.2、proactivity disabled、外部发送关闭。
- Linux 公开 deb 安装 run [35817527851](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35817527851) 成功；Windows 公开 NSIS 安装 run [35817533119](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35817533119) 成功。两者属于干净安装验收，不替代旧版应用内升级。

## 本机安装与数据保持

- 环境：macOS 26.5.1、Apple Silicon。安装前唯一可见 `/Applications/EduPi.app` 为 0.3.29，Core/投影/Kernel ready、Core `d05cf89…`；51 学生、240 任务、43 校历、9 课表；手机入口未启用。认证、模型、设置 SHA-256 分别为 `093af081…`、`37e90c3a…`、`abe47833…`。
- 使用公开 DMG 复制到同卷临时目录并再次通过 `codesign --verify --deep --strict`、`spctl --assess --type execute`、Node V8 命令。v0.3.29 正常退出后保留旧 `.app` 备份，将 v0.3.35 放在原 `/Applications/EduPi.app` 路径并启动；原 v0.3.29 备份位于 `/private/tmp/edupi-v035-preupgrade-backup.OBmTPv`。
- 安装后应用窗口显示 0.3.35，管理中心“系统”页显示 Core `860594a…`、合同 1.1、12/12 命令和已就绪教育投影；`GET /api/edupi/status?summary=1` 回读 Core/投影/Kernel ready，仍为 51/240/43/9，proactivity disabled、`externalSend=false`。三份配置 SHA-256 与安装前完全一致；设置页回读手机未启用、JEV 浏览器决策仍启用、原 URL 和已保存 API Key 状态保持、系统通知已开启。`/Applications` 中只有一个 EduPi.app，签名和 Gatekeeper 复核通过。
- v0.3.29 没有 updater 插件，本次只能一次手动 bootstrap；没有声称旧版完成 Tauri 下载、验签、自动替换流程。v0.3.35 后续应用内升级需下一版本实际发布后再验。

## 仍待验收

- macOS 后台通知点击返回、真实睡眠唤醒、Safe Mode 故障插件注入；Windows/Linux 旧版应用内升级；真实第二台手机与网络切换；JEV 实服/受管浏览器/Core Receipt、OpenConnector 安装版 sidecar；PDF/image/ICS occurrence 提取、逐域教学内容人工核对与真实教师试用。
