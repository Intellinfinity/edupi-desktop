# v0.3.36 应用内更新风险验收

## 风险与修复

- v0.3.29 的公开 macOS 二进制缺 updater 插件，错误码 `UPD-227408f2` 精确对应 `plugin updater not found`；已发布的二进制不能原地补入插件。本机此前用公证 DMG 完成一次 v0.3.29 → v0.3.35 手动 bootstrap，详见 [v0.3.35 安装验收](2026-09-23-v0.3.35-signed-macos-recovery.md)。
- Desktop [#220](https://github.com/Intellinfinity/edupi-desktop/pull/220) 合并为 `71d9670f4f06f9ceb14fd05cd8dcd06ee9737bbe`：正式 Tauri 构建没有 updater 公钥时由 `build.rs` 直接失败；macOS 最终可执行文件还须包含与发布配置完全一致的公钥，检查安排在公证前。本地空公钥负测按预期被构建脚本拒绝。`npm test` 为 1453 项、1427 passed / 26 skipped / 0 failed；TypeScript、lint、npm audit（0）、release verify、actionlint、Cargo metadata 与 28 项 Rust tests 通过。

## 发布证据

- 正式 run [35820263217](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35820263217) 的 macOS、Linux、Windows、manifest 均成功。macOS 从最终 `.app` 启动隔离 Core 成功，`Verify packaged macOS updater key` 明确输出公钥校验通过。
- Release [v0.3.36](https://github.com/Intellinfinity/edupi-desktop/releases/tag/v0.3.36) ID `394308386` 为 Latest，非草稿、非预发布，固定 `71d9670…`，11 项资产。Apple submission `fb9c113d-aad4-461a-8f14-61170f43b1b3` Accepted；runner 的 DMG staple/validate、Gatekeeper 与事务替换通过，最终 DMG SHA-256 `06b01e33e41dfab9babc27d3b731450900a8e9b23994082129ef8561d3b07d40`。
- canonical Raw、旧 Raw、旧 GitHub Latest 三个 endpoint 均返回同一份 v0.3.36 `latest.json`，SHA-256 `e6b474a0f6755240b118fc7de452338b32872faeef4b6c752c2141ff52794598`，7 个平台键且没有空签名。macOS GitHub API 资产 URL 携带 `Accept: application/octet-stream` 请求返回 HTTP 206 和 1024 字节分片；feed commit `64e065b42d762184ad32f0908ae2362ef3f4902e`。

## 本机真实应用内更新

- 环境：macOS 26.5.1、Apple Silicon。安装前 `/Applications/EduPi.app` 为 v0.3.35、进程 PID `10848`，Core/投影/Kernel ready、Core `860594a…`；51 学生、240 任务、43 校历、9 课表，`externalSend=false`。认证、模型、设置 SHA-256 分别为 `093af081…`、`37e90c3a…`、`abe47833…`；旧 `.app` 签名备份在 `/private/tmp/edupi-before-inapp-036.48iSKq/EduPi.app`。
- 在已安装 v0.3.35 的设置页看到 `0.3.35 → 0.3.36` 后点击“更新”。应用完成清单检查，下载进度从 7% 持续增加到 93%，随后显示“正在安装并重启”；没有重试或错误。固定的 `tauri-plugin-updater 2.10.1` 源码在 `Update::download` 返回字节前调用 `verify_signature(&buffer, &self.signature, &self.config.pubkey)?`，验证失败会阻止后续 `install`；这条运行路径已实际进入安装与重启。
- 安装后原路径应用、实际窗口均显示 v0.3.36，新进程 PID `58654`；`GET /api/edupi/status?summary=1` 回读 Core/投影/Kernel ready、Core `860594a…`、51/240/43/9 不变、proactivity disabled、`externalSend=false`。三份配置 SHA-256 与升级前相同；设置页手机入口仍未启用、JEV 浏览器决策仍启用，原 URL 与“已保存 API Key”状态保持，系统通知已开启。`/Applications` 仅有一个 EduPi.app，深度代码签名和 Gatekeeper 均为 `accepted / Notarized Developer ID`，新二进制含 updater 公钥。
- Linux 公开 deb 安装 run [35823858574](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35823858574) 成功，在 Xvfb 中启动真实安装程序并回读 Core/投影 ready、Core `860594a…`。Windows 公开 NSIS 安装 run [35823864062](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35823864062) 成功：安装器退出 0，应用持续运行、workspace HTTP 200，第二次启动复用唯一进程，公开资源的 Cargo 检查通过。两者仍是干净安装，不替代 Windows/Linux 旧版应用内升级。

## 边界

- v0.3.35 起的 macOS 应用内更新链路已从真实安装版验收。v0.3.29 的已发布二进制无法自行注册 updater 插件，v0.3.25–v0.3.28 固化旧 endpoint；这些版本需一次性替换安装。旧 v0.3.30–v0.3.34 macOS 包已撤回为 draft。Windows/Linux 旧客户端应用内更新、通知点击、真实睡眠唤醒和真实第二台手机仍需各自验收，不能用本次 macOS 结果替代。
