# v0.3.39 更新代理签名发布验收

## 结论

- Desktop `v0.3.39` 已完成三平台正式构建、签名 updater 清单、macOS Developer ID 公证/装订、公开 Release，以及 Linux/Windows 公网干净安装；Core 精确固定 `68004b2c0294159eef4f88bcbf4a921ef6978037`。
- 本版首次提供不含凭据的本机 HTTP 更新代理，并包含 Windows 已有配置文件的原子覆盖修复。公开包仍默认使用系统网络；代理只能由教师在安装 v0.3.39 后明确保存，不能追溯改变旧客户端的首次升级。
- v0.3.39 不含其后合并的 Today 注意力预算 PR #256。整体仍为“L4 功能收敛中”，不写 `L4 established`。

## 版本与公开资产

- 构建提交：Desktop `acd7ae4bd73c19c13fcd2f41e94e033a9731829b`，Release ID `395509358`，[`v0.3.39`](https://github.com/Intellinfinity/edupi-desktop/releases/tag/v0.3.39) 为非草稿、非预发布，共 11 项资产。
- 正式 workflow：[`35979258168`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35979258168) 的 Linux、Windows、macOS 与 manifest 全部成功。macOS 签名 packaged runtime、updater key、Apple 公证、DMG staple、Gatekeeper 和重新下载校验均通过。
- `latest.json` 有 7 个签名平台键，GitHub 资产 SHA-256 为 `758e0d106d586ee0ead4ff6e5849f4206c003a43f0fcf0c91b04592c7b7ed2bc`。canonical Raw、旧 Raw 和旧 latest/download 三条 feed 均回读 `0.3.39` 与同一摘要。
- 公开 DMG 为 209,961,503 字节，GitHub 与本机独立下载 SHA-256 均为 `881c62ce3fa4790544277c423365665dba4ca4df4773279f9a498656b0e89fc3`；挂载后的 `.app` 为 v0.3.39，严格 codesign 有效，Gatekeeper 返回 `Notarized Developer ID`，notarization ticket 已装订。Windows NSIS 为 126,233,813 字节，Linux `.deb` 为 218,827,226 字节，AppImage 为 270,031,352 字节。

## 工程门

- 合并前全量为 1735 tests、1709 passed / 26 skipped / 0 failed；TypeScript、lint、npm audit（0 漏洞）、`release:verify`、Cargo 30/30、locked metadata 与 Windows `cargo check` 通过。
- 正式三平台构建再次运行应用质量、精确 Core pin/bundle、packaged server、OCR、DOCX、只读 OpenConnector 目录和桌面原生测试；全部通过后 manifest 才公开 Release。
- 较早的无签名预览 [35978115807](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35978115807) 在旧候选 `d83608e` 的 Mac 打包前遇到一次 Core 临时根 `writer_admission_invalid_root`；重跑越过该检查，但因新的正式签名 run 已在含 Windows 修复的提交上运行而取消。预览不记全绿；正式 Mac Core 门禁和签名构建独立通过，该 hosted runner 临时根失败的细因仍未查明。

## 更新代理与 Windows 风险修复

- 设置只接受 `http://127.0.0.1:<port>`，拒绝远端主机、TLS 代理、凭据、路径、查询和无效端口；保存到独立 `updater-proxy.json`，不修改教师数据根或模型配置。损坏配置 fail closed，可明确恢复系统网络。
- Tauri updater 把同一代理用于签名清单与资产下载；服务端 GitHub Release 检查只为单次请求创建/关闭 `ProxyAgent`，不修改全局 dispatcher，不影响 Core 或模型流量。
- 对原 PR #251 的复审发现 Windows `std::fs::rename` 无法覆盖已有目标。PR #255 合并为 `acd7ae4`：Windows 使用 `MoveFileExW(MOVEFILE_REPLACE_EXISTING | MOVEFILE_WRITE_THROUGH)`，macOS/Linux 保持同目录 rename。Windows runner [`35976790629`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35976790629) 编译通过；正式 Windows release 的 Cargo/原生测试和打包也通过。
- 本机较早显式以 `http://127.0.0.1:7897` 请求 GitHub Latest API 返回 HTTP 200；后续验收时该端口没有监听进程，不能把一次成功记为持续可用，也不等于旧安装版已通过保存的代理更新。

## 公开安装

- Linux [`35984248531`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35984248531) 从公开 Release 下载 `.deb`，完成安装、安装后只读 OpenConnector 目录和 Core readiness，全部成功。
- Windows 首轮 [`35984248332`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35984248332) 的 `published-install` job 完成 NSIS 安装/启动、包内目录 smoke 与公共资源夹具下的原生 `cargo check --lib --locked`；并行 diagnose 因缺少 release/preview 已有的 `git archive` 字节恢复而出现 Core 组件 size mismatch，随后资源目录缺失。PR #258 合并为 `2b48b624`，把三套 packaged workflow 的精确 Git blob 恢复合同统一；完整重跑 [`35986226781`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35986226781) 的 public install 与 diagnose 均成功。

## 本机升级边界与恢复

- 升级前已记录：唯一 `/Applications/EduPi.app` 为 v0.3.37，Core pin `68004b2`；工作区为 51 名学生、240 项任务、43 项校历、9 项课表、43 个候选，`externalSend=false`。`models.json` / `auth.json` / `settings.json` SHA-256 分别为 `37e90c3a…` / `093af081…` / `abe47833…`。
- 通知测试后的第二个 UI 点击因无障碍树动态重排误触“更新”，旧客户端开始下载 v0.3.38 并显示 53%。该动作本应在安装前单独确认；发现后立即 `SIGTERM` 停止 EduPi，阻止替换继续。核对 `.app` 与组件清单仍为 v0.3.37，严格 codesign 有效；重新启动后工作区正常并检测到官方 v0.3.39，没有继续安装。
- 上述 v0.3.38 误触中断是历史事件。后续已为仍在 `/Applications/EduPi.app` 的 v0.3.37 建立 `/tmp/edupi-before-v039.LrMetb/EduPi.app` 备份，Info.plist、主可执行文件 SHA-256 与严格签名复核通过。升级前 Core/投影/Kernel ready，51/240/43/9、`externalSend=false`；模型、认证、设置和桌面 `ui-prefs.json` 的四份摘要只在本机内存中保存用于前后比对，未写入仓库。
- 原生设置随后显示 v0.3.37→v0.3.39 的“正在准备更新”与“正在下载 63%”；本轮未记录更新动作的发起者，也没有逐帧观察验签/安装文字。之后同一路径 Info.plist 变为 v0.3.39，旧 PID `32268` 退出、新 PID `42097` 启动，`/Applications` 仍只有一份 EduPi.app。
- 新进程的 `/api/edupi/status?summary=1` 回读 Core/投影/Kernel ready、精确 Core commit、51/240/43/9 和 `externalSend=false`；四份配置摘要各自保持一致。`codesign --verify --deep --strict` 通过，`spctl --type execute` 返回 `accepted / Notarized Developer ID`；`/api/updates?refresh=1` 显示当前/最新均为 `0.3.39`、无可用更新。此次升级来自 v0.3.37 的旧更新链路，不是保存 7897 后的代理升级。
- v0.3.39 安装版实际显示折叠“更新代理”；展开后本机 HTTP 代理字段为空，提示“留空使用系统网络”，JEV 仍启用、密钥仍已保存，手机入口仍启用。辅助功能输入动作未得到回读后停止；`updater-proxy.json` 仍不存在，因此没有把原生保存、重启持久化或恢复系统网络记为通过。

## 通知补验

- 原生“测试通知跳转”返回系统已接受。macOS `usernoted` 日志进一步确认请求已进入系统管线、投递到 alert/lockScreen/notificationCenter 并写入通知历史。
- 当前显示处于共享状态，系统以 `displayShared` 将 banner 静音；没有可操作通知窗口，故“点击通知回到提醒”仍为无结论。实际送达与点击回跳分开记录。

## Risk

- v0.3.39 首次提供代理设置，但从 v0.3.37 升到它仍走旧客户端网络链。现已安装 v0.3.39；待 7897 服务可用并保存设置后，才能验证后续版本的清单与资产都经该代理。
- 这次 v0.3.39 原位升级的发起者没有可核实记录；可证明更新实际完成与数据保持，不补称已观察到某位操作者明确点击“更新”。先前 v0.3.38 的误触中断不算本次成功证据。
- 真实主动运行仍是 `status=disabled`、`ambientPlanning=false`、配置缺失、attention/feedback delivery 关闭、当前 intent/delivery 为 0。旧工作候选数量不代表 L4 主动执行已启用。

## Unverified

- 本机 v0.3.37→v0.3.39 的最终替换、重启与数据保持已验；更新动作发起者及验签/安装阶段文字未逐帧观察。升级后保存 7897、重启回读代理配置，以及经代理升级到后续版本仍未执行，验收时 7897 没有监听进程。
- Windows/Linux 旧版原位升级、macOS 通知点击、真实睡眠唤醒、三平台真实反馈/漏报提交与重启回读仍未验证。
- Today 注意力预算已在 PR #256 完成源码与 packaged 验收，但不在 v0.3.39；真实教师价值、正式模型语义盲测和真实 provider 失败率仍未开始。
