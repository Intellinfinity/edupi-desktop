# v0.3.40 Today 注意力预算签名发布验收

## 结论

- Desktop `v0.3.40` 已完成三平台正式构建、签名 updater 清单、macOS Developer ID 公证/装订、公开 Release，以及 Linux/Windows 公网干净安装；Core 精确固定 `68004b2c0294159eef4f88bcbf4a921ef6978037`。
- 本版把 Today 每列默认 4 项的注意力预算带入签名安装包，不修改 43 个候选的 Core 状态、来源或教师决定。
- 本机已从 v0.3.39 经持久化的 7897 代理完成原位下载、验签、覆盖安装和自动重启；升级后 Today 注意力预算的默认折叠、展开和收起均已操作。整体仍保持“L4 功能收敛中”，不以安装版通过替代真人价值证据。

## 版本与公开资产

- 正式 workflow：[`35993787607`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35993787607)，构建目标 `c5460ce7608d7decff75f333ca8c27c91bfc6e1c`；Linux、Windows、macOS、manifest 全部成功。发布准备 PR [#260](https://github.com/Intellinfinity/edupi-desktop/pull/260) 合并为 `f49cc101`，之后仅含验收文档的 #261 进入精确构建目标。
- Release ID `395608582`，[`v0.3.40`](https://github.com/Intellinfinity/edupi-desktop/releases/tag/v0.3.40) 为非草稿、非预发布、当前 Latest，共 11 项资产。`latest.json` 有 7 个签名平台键，GitHub 资产与 canonical Raw、旧 Raw、latest/download 三条 feed 均为 `0.3.40`，SHA-256 同为 `b3605f88efb75e0d29d47bdb9e71dd2b12536e62ce50bcc321a792898e5bf797`。
- 公开 DMG 为 209,955,075 字节，GitHub 声明与本机独立下载 SHA-256 均为 `52463d58850f163470564c6b0558c92eb552acfe5414ede4787cde25c7fa77fe`。Windows NSIS 为 126,228,550 字节，Linux `.deb` 为 218,824,140 字节，AppImage 为 270,031,352 字节。

## 工程门

- 发布分支全量为 1737 tests、1711 passed / 26 skipped / 0 failed；TypeScript、lint、npm audit（0 漏洞）、`release:verify`、Cargo locked metadata 与 30/30 测试通过。PR [#260](https://github.com/Intellinfinity/edupi-desktop/pull/260) 合并前 Node 与 RustSec 审计 run [`35993412304`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35993412304) 全绿。
- 正式三平台构建重新运行应用质量、精确 Core pin/bundle、packaged server、OCR、DOCX、只读 OpenConnector 目录和桌面原生测试；macOS 另通过包内运行、updater 公钥、Apple 公证、DMG staple 和 Gatekeeper 后才公开 manifest。
- 公开 Linux 安装 run [`35998790991`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35998790991) 成功；它从 Release 下载 `.deb`，安装后通过只读目录与 Core readiness。Windows 完整 run [`35998805393`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35998805393) 的 `published-install` 与 `diagnose` 均成功；额外 native-only run [`35998802947`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/35998802947) 也成功。

## macOS 公开安装包

- 独立下载的 DMG 挂载后应用版本为 `0.3.40`；`codesign --verify --deep --strict` 通过，Gatekeeper 返回 `accepted / Notarized Developer ID`。
- 本机 `stapler validate` 再次因 Apple CloudKit TLS `-1200` 无结论；它不覆盖正式 macOS runner 的 notarization/staple 成功与本机 Gatekeeper 接受。
- DMG 经 `http://127.0.0.1:7897` 下载时出现一次 `curl (18) Transferred a partial file`；原请求的自动重试从头开始，随后以 byte-range 从 79,605,760 字节处续传完成，最终大小和摘要精确匹配。这证明代理可交付公开大资产，也保留了当时连接抖动的真实风险。

## 本机代理升级与 Today

- 已安装 v0.3.39 在保存 7897 并退出重启后，强制 Release 查询返回当前 `0.3.39`、最新 `0.3.40`、`updateAvailable=true`；Clash 同时记录到 `api.github.com:443` 的代理连接。
- 升级前唯一 `/Applications/EduPi.app` 仍为 v0.3.39，主进程 PID `77509`；Core/投影/Kernel ready、51/240/43/9、`external_send=false`。`models.json` / `auth.json` / `settings.json` / `ui-prefs.json` SHA-256 分别为 `37e90c3a…` / `093af081…` / `abe47833…` / `48a5eb0f…`，代理配置为 `c8eefffd…`。临时回退副本为同一 v0.3.39、同一 Info.plist/主程序摘要，严格签名通过。
- 第一次原生更新到 2% 后返回“更新包下载中断”，v0.3.39 和数据未改。对 updater 精确 macOS 资产端点的 1 MiB range 请求随后经 7897 返回 `206`；同一已确认安装的第二次尝试从 1%→99% 连续完成。下载期间 `lsof` 显示 PID `77509` 到 `127.0.0.1:7897` 的 ESTABLISHED 连接；页面进入“正在安装并重启”后，旧 PID 退出、新 PID `85846` 启动。
- 重启后唯一安装副本为 v0.3.40；严格 codesign 通过，Gatekeeper 返回 `accepted / Notarized Developer ID`。Core `68004b2`、组件清单、Runtime 清单、Core/投影/Kernel ready、51/240/43/9、`external_send=false` 均保持；上述五份配置摘要逐份不变，设置页仍回读 `http://127.0.0.1:7897`。强制更新检查返回当前/最新均为 `0.3.40`、无可用更新。
- 真实 Today 仍有 31 项“待你决定”与 12 项“稍后处理”；安装后默认分别显示 4 项，优先显示距今天最近的 9/24、9/23 事项，并显示“查看其余 27 项”/“查看其余 8 项”。两列的展开与收起均在签名安装版实际操作通过；候选总数与 Core 状态未改。源码与 packaged 证据见 [Today 注意力预算验收](2026-09-24-today-attention-budget.md)。

## Risk

- 7897 在轻量 GitHub API、公开 DMG 和最终 Tauri updater 全链中已成功，但实测仍有一次 TLS 超时、一次手工下载 partial transfer 和一次应用内 2% 中断。第二次应用内更新成功，说明有界重试路径可恢复，不说明当前代理节点没有运营抖动。
- 真实根的主动运行仍为 `status=disabled`、`ambientPlanning=false`、配置缺失、attention/feedback delivery 关闭。当前课表只有 `class_name=703` 而没有稳定 `class_id`，同范围材料为 0；继续 fail closed 是正确的安全行为。

## Unverified

- Windows/Linux 旧版原位升级仍未实测，公网干净安装不能替代。
- macOS 通知点击、真实睡眠唤醒、三平台真实反馈/漏报提交与重启回读仍未验证。通知送达已通过，屏幕共享导致 `displayShared` 静音时没有可点击 banner。
- 真实教师连续试用与价值由用户组织；正式付费模型盲测仍缺明确预算与独立裁决者。当前 60 故事 / 360 时点 / 120 重复项的 492 次非付费规则诊断保持 0 provider/model calls，不冒充正式语义评估。
