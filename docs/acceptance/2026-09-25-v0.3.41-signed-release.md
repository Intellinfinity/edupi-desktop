# v0.3.41 提醒读取状态签名版验收

## 结论

- Desktop [#265](https://github.com/Intellinfinity/edupi-desktop/pull/265) 的提醒加载与失败状态修正已由发布准备 [#266](https://github.com/Intellinfinity/edupi-desktop/pull/266) 带入 v0.3.41。发布目标为 merge `ce7689b04ab37ddc2ef885b4602b056f7ea4d84c`；Core 仍精确固定 `68004b2c0294159eef4f88bcbf4a921ef6978037`，没有在构建中途切换合同。
- 正式 [Release v0.3.41](https://github.com/Intellinfinity/edupi-desktop/releases/tag/v0.3.41) 已公开并为 Latest；本机从唯一的 `/Applications/EduPi.app` v0.3.40 走已保存的 `http://127.0.0.1:7897` 原生更新、下载、验签、安装和自动重启，未手动覆盖应用。安装版已打开提醒页，显示 11 条待处理而非错误空状态。

## 发布与公开包

- 正式 workflow [`36088969006`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/36088969006) 的 release、Linux、Windows、macOS 与 manifest jobs 全绿；Apple 凭据预检、macOS `.app`/DMG 公证装订及 Gatekeeper 步骤通过后才发布。Release ID `396257693`，非草稿、非预发布，11 项资产。canonical Raw feed v0.3.41 有 7 个平台键、签名均非空、资产 URL 均为 GitHub API；`latest.json` SHA-256 `8eecdfaacfc424cd9afdb5a0e8fb92042d91a181e6d5ed82a6b66b405362bce7` 与 Release digest 一致。
- 公网 API 资产经 7897 独立下载的 DMG 为 `209955005` 字节，SHA-256 `9e0c2ee28d70131dae9073c2b2b387aec99acc30bf267df55dabf4ff203d2933`，与 Release 声明相同。挂载应用为 v0.3.41，组件清单 SHA-256 `85188cfac8a7e57383a3452e8adb43988f3f6c731ee89b4fe6fb513687a33ac0` 与 Release 相同；`codesign --verify --deep --strict` 通过，DMG `spctl --type open --context context:primary-signature` 与 `.app` `spctl --type execute` 均为 `accepted / Notarized Developer ID`。本机 `stapler validate` 因 Apple CloudKit TLS `-1200` 无结论，不替代 runner 的装订成功证据，也不将本机结果记为通过。
- 公网 Linux 安装 run [`36092599954`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/36092599954) 全绿；Windows run [`36092599710`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/36092599710) 的 published-install 与 diagnose 均全绿。它们证明公开包干净安装，不代表旧客户端原位升级。

## 本机原位升级

- 升级前 `/Applications/EduPi.app` 为 v0.3.40，主 PID `85846`；精确副本 `/tmp/edupi-before-041.ub4Olg/EduPi.app` 保留为回退资产，版本、主程序 SHA-256 和严格签名与原安装版一致。升级后主 PID `53710`，同一应用路径版本为 v0.3.41，应用严格签名与 Gatekeeper 公证均通过；`/Applications` 仅有一个 `EduPi.app`。
- 升级前后 Core/投影/Kernel 均 ready，Core commit `68004b2`、`externalSend=false`，投影计数保持学生 51、课表 9、校历 43、任务 240。`models.json`、`auth.json`、`settings.json`、`ui-prefs.json`、`updater-proxy.json` 的 SHA-256 分别逐项保持 `37e90c3a…`、`093af081…`、`abe47833…`、`48a5eb0f…`、`c8eefffd…`；设置回读代理仍为 `http://127.0.0.1:7897`。安装版组件清单摘要与公开包相同，更新接口无更新项。
- 安装版提醒入口实际打开并回读待处理 11、稍后提醒 0、已移除 50；本次未修改真实教师事项。首次读取与失败空态的隔离页面行为回归见 [提醒工作台验收](2026-09-20-reminder-workbench.md)，不能把本次只读打开冒充安装版故障注入测试。

## 工程门与仍未验证

- 发布准备时 `npm test` 1739 total / 1713 passed / 26 skipped / 0 failed；TypeScript、lint、`npm audit --audit-level=high`（0 漏洞）、`release:verify`、Cargo locked metadata 与 `cargo test --locked` 30/30 通过。PR #266 的 Node 与 RustSec CI 审计全绿。
- macOS 共享屏幕通知临时放行后，系统日志确认 v0.3.41 测试通知展示为横幅并加入历史；自动化未能暴露可点击通知，故点击返回提醒仍未验收。原全局选项“关闭通知”已恢复并回读，见 [提醒工作台验收](2026-09-20-reminder-workbench.md)。
- v0.3.41 签名应用在 `/tmp/edupi-sleep-041.QE5OpT` 隔离 HOME/教师数据/Pi agent 环境启动，只有 loopback listener；Core `68004b2`、投影/Kernel ready，学生/课表/校历/任务均为 0，`externalSend=false`。`pmset sleepnow` 后，`pmset -g log` 记录 13:15:36 Software Sleep、13:16:07 DarkWake、13:21:31 FullWake（键盘/UserActivity）。隔离主 PID `85356` 保持、Core 恢复 ready，G1 `last_timer_check_at` 更新为 `2026-09-25T05:21:31.325Z`，队列仍 0、Kernel 成功总数仍 2，没有重复执行。`pmset schedule wake` 因缺管理员权限未设置，也未更改原有系统排程。
- 隔离应用正常退出后，真实配置五份 SHA-256 与升级前逐项一致；正常启动的真实 EduPi PID `87783`、Core/投影/Kernel ready、51/9/43/240 与 `externalSend=false` 保持，原先启用的局域网监听也恢复。此实测证明签名应用跨真实整机睡眠后存活并补做 G1 定时检查；隔离根没有到期任务，不证明某个课前产物/通知恰好跨截止时刻补跑，也不替代通知点击。当前 Core 固定版本两次合成题真实模型核对见 [AI 协作验收](2026-09-23-ai-collaboration-composer.md)；正式真实材料盲评与 Windows/Linux 旧版原位升级仍未执行。
