# v0.3.42 Core #191 配对签名版验收

## 状态

- 正式发布 run [`36110749260`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/36110749260) 从 Desktop merge `ad8ab76d3715d62256cf7e5950e142ed0bb4b11b` 全绿完成；[Release v0.3.42](https://github.com/Intellinfinity/edupi-desktop/releases/tag/v0.3.42) 已公开。Linux/Windows 公网安装和本机 macOS 应用内升级均通过下述边界的验收。
- 目标 Core 为 `86a49de9278704b3b64ad637c13acc03f21d34b5`。源码、隔离 Core 和 staged 证据见 [Core #191 配对验收](2026-09-25-core-191-desktop-pairing.md)。G2 与共享执行器保持 `activation_pending`，`external_send=false`。

## 升级前基线

- 本机唯一 `/Applications/EduPi.app` 为 v0.3.41，严格代码签名和 Gatekeeper `accepted / Notarized Developer ID` 均通过；主进程 PID `87783`。已保留精确应用副本 `/tmp/edupi-before-042.tVvZue/EduPi.app`，版本和严格签名重新核对通过。
- 原安装版 `/api/edupi/status?summary=1` 实际回读 Core、教育投影和 Kernel `ready`，Core commit `68004b2c0294159eef4f88bcbf4a921ef6978037`，学生 51、课表 9、校历 43、任务 240，`externalSend=false`。
- 升级前 `models.json`、`auth.json`、`settings.json`、`ui-prefs.json`、`updater-proxy.json` 的 SHA-256 依次为 `37e90c3a93928531acee18bf34cdb73b7fe5e0b5a8ce982c6b88ca74ec5cb32d`、`093af081468430b3cec640183e14ba9e4a17fb7dfb8c02571240f8af5c6c7259`、`abe4783379a997231f8e5078092e530468f1f1c56638bca79c42d62c8c631b83`、`48a5eb0f3a382c8728239599346adea8beaa1d1f9914f88c2bee0603a92213b5`、`c8eefffd39f7c730eab0360f6f066200fe437710f1e1a686c45793828cfe02a2`。
- 原生管理中心“系统”实际显示 v0.3.41 与 Core `68004b2`；没有用开发页面冒充安装版。

## 正式发布与公开包

- Release ID `396420237`，非草稿、非预发布，11 项资产。canonical Raw feed v0.3.42 有 7 个平台键、签名均非空、资产 URL 均为 GitHub API；Raw `latest.json` SHA-256 `1e30434c9796ea0921b932ffc22433b36eee36248b0585ea50186492be9d4203` 与 Release digest 一致。构建中的草稿仅在三平台和 manifest 全绿后才公开。
- macOS runner 中 DMG 单独提交 Apple 公证后返回 `Accepted`；`stapler staple`/`validate`、`.app` 与 DMG Gatekeeper 均通过。经保存的 7897 代理从公开 GitHub API 资产端点独立下载 `209969416` 字节 DMG，SHA-256 `6e8fe6eb9cc86b98622c1207ce84a40ae864cdc34d2f9d5792bbc9563a1b39f7` 与 Release digest 一致。只读挂载应用为 v0.3.42，严格签名和 `.app`/DMG Gatekeeper 均为 `accepted / Notarized Developer ID`；组件清单摘要 `d1df40e6fd5a1d0e1e3bf6ac66e1a0b5b61d800a7b0a7d52826136e4ee470f42` 也与 Release 相同，随后已卸载 DMG。
- 本机 `xcrun stapler validate` 查询 Apple CloudKit 时 TLS `-1200`，无结论；不把该本地检查说成通过，也不推翻 runner 的实际装订/验证成功和本机 Gatekeeper 接受结果。Linux 公网干净安装 run [`36118419926`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/36118419926) 成功；Windows 公网安装及后置原生诊断 run [`36118419829`](https://github.com/Intellinfinity/edupi-desktop/actions/runs/36118419829) 全部成功。这两项不是 Windows/Linux 旧客户端应用内原位升级。

## 本机原位升级与界面

- 唯一 `/Applications/EduPi.app` 从 v0.3.41 的 PID `87783` 在现有设置页检测 v0.3.42，经已保存的 `http://127.0.0.1:7897` 代理下载、验签并进入“正在安装并重启”；新主 PID `7368`，同路径版本 v0.3.42，没有新增 `/Applications` 副本。安装版严格签名和 Gatekeeper `accepted / Notarized Developer ID` 均通过。
- 重启后的 `/api/edupi/status?summary=1` 为 Core、投影、Kernel `ready`，Core commit 精确为 `86a49de9278704b3b64ad637c13acc03f21d34b5`，学生/课表/校历/任务仍为 `51/9/43/240`，`externalSend=false`；五份配置的 SHA-256 与升级前逐项一致。管理中心系统页实际显示 v0.3.42、Core 与 Runtime 清单哈希，以及“课前准备可运行 · 学生跟进待接入 · 其他任务待接入”；自动运行页没有整机自动检查异常，实际呈现个别课前任务缺少材料。
- 安装版通用设置中 OpenConnector 只读目录展开后搜索 `calendar`，返回 10 个 Action；未触发外部 Action。人工点选 `get_calendar` 参数时桌面窗口被用户切换，inspect 未读到最终结果，故安装版人工 inspect 仍未验收；隔离 staged 的 search/inspect/execute 拒绝测试见 [Core #191 配对验收](2026-09-25-core-191-desktop-pairing.md)。

## 保留边界

- G2 与共享能力继续 `activation_pending`，不因 Core #191 合同可用就声称学生跟进已经运行。OpenConnector 真实 Action、Core capability/Receipt、JEV 受管浏览器执行器仍未实现；Windows/Linux 旧版应用内升级未验。

教师已将系统通知手动点击和真实材料模型盲评延后；不把它们记入本版通过条件，也不从原路线图删除。
