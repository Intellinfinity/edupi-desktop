# v0.3.44 OpenConnector 独立页与 JEV 意图预检发布验收

## 发布状态

- Desktop [#274](https://github.com/Intellinfinity/edupi-desktop/pull/274) 已合并为 `0af3f6eab53a356a82a7f29686097cbe0451898a`；正式三平台 [run 36139803358](https://github.com/Intellinfinity/edupi-desktop/actions/runs/36139803358) 全绿，[Release v0.3.44](https://github.com/Intellinfinity/edupi-desktop/releases/tag/v0.3.44) 非 draft、非 prerelease，目标提交相同。Release ID `396620288`，11 项资产，7 个 updater 平台键。
- Linux、Windows、macOS 构建成功。macOS runner 的包内运行、DMG 公证装订和 `.app`/DMG Gatekeeper 核验步骤均为 success。Raw feed `latest.json` 为 0.3.44，七键签名非空且 URL 全部是 canonical GitHub API asset；本机直连下载 SHA-256 `fee9d824712e42870f5830fb94705f073f7ee48959f79333a75c556080fe6848`，与 Release digest 一致。
- Linux [公开安装 run 36144954871](https://github.com/Intellinfinity/edupi-desktop/actions/runs/36144954871) 已成功；Windows [公开安装与诊断 run 36144954663](https://github.com/Intellinfinity/edupi-desktop/actions/runs/36144954663) 的两个 job 也全部成功。公开 DMG 独立下载为 `209965487` 字节、SHA-256 `f4d4d97583ffd7072fcac36224d9d2128f2a50117a911d045b47d8b66f7613ce`，与 Release digest 一致。只读挂载应用为 0.3.44，内部组件清单 SHA-256 `23d5cb82164cec9bc45eb61d60836f58303c3e345e8401ff52fbbe2f10e46b2e` 与公开组件资产一致；`codesign --verify --deep --strict` 通过，DMG `spctl --type open --context context:primary-signature` 和 `.app` `spctl --type execute` 均为 `accepted / Notarized Developer ID`，镜像已卸载。本机 `xcrun stapler validate` 因 Apple CloudKit TLS `-1200` 无结论，不覆盖 runner 的装订成功证据。
- 公开 DMG 卷上直接启动 OpenConnector catalog host 首次超过测试的 15 秒启动限额；将同一公开包内 `open-connector` 与签名 Node helper 复制到隔离普通目录后，`npm run test:staged-openconnector` 在 5.6 秒内全部通过，包含 providers/actions/inspect/scoped search 与 execute 阻断。压缩卷读取时延是推断，不能把第一次超时改写为通过。本机 `/Applications/EduPi.app` 仍为 0.3.42，未把新页面记为安装版通过。

## 功能证据与边界

- 新页面、headless 官方包目录、JEV 21 条合成语义预检的源码/隔离/实服证据见 [本批验收](2026-09-25-openconnector-admin-jev-intent-preflight.md)。21/21 只针对合成短句；正式独立教师双裁决盲测 `not_run`，JEV 仍不参与对话。
- OpenConnector 页面是 EduPi 自建只读目录后台，不是上游完整 Console；真实账号、OAuth、外部 Action、Core grant/Receipt 继续关闭。发布流程与隔离运行均验证 `execute` 请求被拒。
- 本机现有更新代理设为 `127.0.0.1:7897`；本轮代理 TCP 可连接，但通过代理访问 Raw feed TLS 超时，直连返回 HTTP 200。Mac 锁屏期间不修改用户配置、不绕过锁屏、不手动覆盖安装。后续原位更新必须再次核对唯一副本、Core `86a49de`、51 学生/240 任务/43 校历/9 课表和模型/认证/设置摘要。

## 未验收

- [x] Windows 公网安装及诊断最终通过。
- [x] 公开 DMG 的 SHA-256 与 Release digest 相同；只读挂载后的版本、严格代码签名、`.app`/DMG Gatekeeper 通过。runner 装订复核通过，本机 stapler 网络失败未单独通过。
- [ ] 从现有 v0.3.42 原位下载、验签、安装、重启；新签名 WebView 在 1440×900 与 800×900 实际完成服务选择、Action 搜索和参数查看，返回旧页面后状态正确，真实执行仍被拒；教师数据与配置不变。
- [ ] Windows/Linux 旧版本应用内原位升级、真实学校设备、Core 权威外部 Action 闭环和正式教师语义盲测另按路线图验收；公开干净安装不替代这些步骤。
